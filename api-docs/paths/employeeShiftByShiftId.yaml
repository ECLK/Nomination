get:
  tags:
    - EmployeeShifts
  description: Returns an employeeshift given a shift Id
  produces:
    - application/json
  parameters:
    - $ref: '#parameters/merchantId'
    - $ref: '#parameters/shiftId'
  responses:
    200:
      description: employeeShift