get:
  tags:
    - EmployeeShifts
  description: Returns list of employeeShift of a merchant
  produces:
    - application/json
  parameters:
    - $ref: '#parameters/merchantId'
    - $ref: '#parameters/limit'
  responses:
    200:
      description: An array of employeeShift